# Runtime and development dependencies are declared in pyproject.toml.
# Install with:
#
#     pip install -e '.[dev]'
#
# This file is kept as a shim so that `pip install -r dev-requirements.txt`
# still works for muscle-memory callers.
-e .[dev]
