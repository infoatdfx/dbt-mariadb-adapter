kind: Features
body: Refactor incremental materialization to dispatch on `incremental_strategy` and add `append`, `merge` (INSERT … ON DUPLICATE KEY UPDATE), and `microbatch` (event_time-windowed DELETE + INSERT) alongside the existing `delete+insert`.
time: 2026-04-21T22:05:00Z
custom:
  Author: claude
